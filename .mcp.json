{
  "mcpServers": {
    "moodle": {
      "command": "uv",
      "args": ["run", "--directory", "mcp_server", "moodle-mcp"],
      "env": {
        "MOODLE_URL": "http://localhost:8080",
        "MOODLE_TOKEN": "${MOODLE_TOKEN}"
      }
    }
  }
}
